<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="org.zerock.myapp.mapper.ReplyMapper">

    <select id="selectReply" resultType="org.zerock.myapp.domain.ReplyDTO">
        SELECT /*+ tbl_reply desc */ * 
        FROM TBL_REPLY
        WHERE BNO=#{bno}
    </select>
        
    <insert id="insertReply">
        INSERT INTO TBL_REPLY(bno, reply, replyer)
        VALUES (#{bno}, #{reply}, #{replyer})
    </insert>

    <update id="updateReply">
        UPDATE TBL_REPLY
        SET REPLY = #{reply}, UPDATEDATE=CURRENT_DATE
        WHERE RNO=#{rno}
    </update>

    <!-- <select id="getIdName" resultType="org.zerock.myapp.domain.ReplyDTO">
        SELECT * FROM TBL_REPLY 
        WHERE rno = #{rno};
    </select> -->

    <select id="ListWithPaging" resultType="org.zerock.myapp.domain.ReplyDTO">
        SELECT /*+ index tbl_reply asc*/ * 
        FROM TBL_REPLY
        WHERE BNO = #{bno}
    </select>

    <select id="countReply" resultType="int">
        SELECT count(*) FROM tbl_reply
        WHERE bno = #{bno}
    </select>
  </mapper>